import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";

// This file contains broad content and integration regressions. The remaining tests are unchanged.
// NOTE: content before the modified section is preserved in the repository by this replacement only if complete content is supplied.
